import { desc } from "drizzle-orm"
import { redirect } from "next/navigation"
import Link from "next/link"
import { createClient } from "@/lib/supabase/server"
import { db } from "@/lib/db"
import { orders, products } from "@/lib/db/schema"
import { formatPKR } from "@/lib/store-data"

export default async function AdminPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/sign-in");if(user.email?.toLowerCase()!==process.env.ADMIN_EMAIL?.toLowerCase())redirect("/account");const [orderRows,productRows]=await Promise.all([db.select().from(orders).orderBy(desc(orders.createdAt)),db.select().from(products).orderBy(desc(products.createdAt))]);const revenue=orderRows.reduce((sum,order)=>sum+order.total,0);return <main className="min-h-svh bg-secondary"><header className="border-b border-border bg-foreground text-background"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4 md:px-6"><Link href="/" className="font-serif text-2xl font-black">ReLace.</Link><div className="text-xs font-bold uppercase tracking-widest">Store admin · {user.user_metadata?.name ?? user.email}</div></div></header><section className="mx-auto max-w-7xl px-4 py-10 md:px-6"><p className="text-xs font-bold uppercase tracking-widest text-primary">Operations overview</p><h1 className="mt-2 font-serif text-5xl font-black">The day at a glance.</h1><div className="mt-8 grid grid-cols-2 gap-4 md:grid-cols-4">{[["Orders",orderRows.length],["Revenue",formatPKR(revenue)],["Live pairs",productRows.filter(p=>p.stock>0).length],["Payment checks",orderRows.filter(o=>o.paymentMethod==="bank"&&o.paymentStatus==="pending").length]].map(([label,value])=><div key={label} className="bg-background p-5"><p className="text-xs font-bold uppercase tracking-widest text-muted-foreground">{label}</p><p className="mt-3 font-serif text-3xl font-black">{value}</p></div>)}</div><div className="mt-8 grid gap-8 lg:grid-cols-[1.3fr_0.7fr]"><div className="bg-background p-5"><div className="flex items-center justify-between"><h2 className="font-serif text-3xl font-black">Recent orders</h2><span className="text-xs font-bold uppercase tracking-widest">Live database</span></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[620px] text-left text-sm"><thead className="border-b border-border text-xs uppercase text-muted-foreground"><tr><th className="py-3">Order</th><th>Customer</th><th>City</th><th>Payment</th><th>Status</th><th className="text-right">Total</th></tr></thead><tbody>{orderRows.length===0?<tr><td colSpan={6} className="py-10 text-center text-muted-foreground">Orders will appear here after checkout.</td></tr>:orderRows.map(order=><tr key={order.id} className="border-b border-border"><td className="py-4 font-bold">{order.orderNumber}</td><td>{order.customerName}</td><td>{order.city}</td><td className="capitalize">{order.paymentMethod}</td><td className="capitalize">{order.status}</td><td className="text-right font-bold">{formatPKR(order.total)}</td></tr>)}</tbody></table></div></div><div className="bg-background p-5"><div className="flex items-center justify-between"><h2 className="font-serif text-3xl font-black">Inventory</h2><Link href="/admin/products/new" className="text-xs font-bold uppercase tracking-widest underline">Add product</Link></div><div className="mt-5 flex flex-col gap-3">{productRows.map(product=><div key={product.id} className="flex items-center justify-between border-b border-border pb-3"><div><p className="font-bold">{product.name}</p><p className="text-xs text-muted-foreground">{product.size} · {product.condition}</p></div><span className={`text-xs font-bold uppercase ${product.stock?"text-accent":"text-destructive"}`}>{product.stock?"Live":"Sold"}</span></div>)}</div></div></div></section></main>}
