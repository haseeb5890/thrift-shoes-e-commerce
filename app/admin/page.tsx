import { desc } from "drizzle-orm"
import Link from "next/link"
import { db } from "@/lib/db"
import { orders, products } from "@/lib/db/schema"
import { formatPKR } from "@/lib/store-data"

export default async function AdminPage() {
  const [orderRows, productRows] = await Promise.all([
    db.select().from(orders).orderBy(desc(orders.createdAt)),
    db.select().from(products).orderBy(desc(products.createdAt)),
  ])
  const revenue = orderRows.reduce((sum, order) => sum + order.total, 0)

  return <section className="mx-auto max-w-7xl px-4 py-10 md:px-6"><p className="text-xs font-bold uppercase tracking-widest text-primary">Operations overview</p><h1 className="mt-2 font-serif text-5xl font-black">The day at a glance.</h1><div className="mt-8 grid grid-cols-2 gap-4 md:grid-cols-4">{[["Orders",orderRows.length],["Revenue",formatPKR(revenue)],["Live pairs",productRows.filter(p=>p.stock>0).length],["Payment checks",orderRows.filter(o=>o.paymentMethod==="bank"&&o.paymentStatus==="pending").length]].map(([label,value])=><div key={label} className="bg-background p-5"><p className="text-xs font-bold uppercase tracking-widest text-muted-foreground">{label}</p><p className="mt-3 font-serif text-3xl font-black">{value}</p></div>)}</div><div className="mt-8 grid gap-8 lg:grid-cols-[1.3fr_0.7fr]"><div className="bg-background p-5"><div className="flex items-center justify-between"><h2 className="font-serif text-3xl font-black">Recent orders</h2><Link href="/admin/orders" className="text-xs font-bold uppercase tracking-widest underline">Manage all orders</Link></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[620px] text-left text-sm"><thead className="border-b border-border text-xs uppercase text-muted-foreground"><tr><th className="px-3 py-3">Order</th><th className="px-3 py-3">Customer</th><th className="px-3 py-3">City</th><th className="px-3 py-3">Payment</th><th className="px-3 py-3">Status</th><th className="px-3 py-3 text-right">Total</th></tr></thead><tbody>{orderRows.length===0?<tr><td colSpan={6} className="px-3 py-10 text-center text-muted-foreground">Orders will appear here after checkout.</td></tr>:orderRows.slice(0,8).map(order=><tr key={order.id} className="border-b border-border"><td className="px-3 py-4 font-bold">{order.orderNumber}</td><td className="px-3 py-4">{order.customerName}</td><td className="px-3 py-4">{order.city}</td><td className="px-3 py-4 capitalize">{order.paymentMethod}</td><td className="px-3 py-4 capitalize">{order.status}</td><td className="px-3 py-4 text-right font-bold">{formatPKR(order.total)}</td></tr>)}</tbody></table></div></div><div className="bg-background p-5"><div className="flex items-center justify-between"><h2 className="font-serif text-3xl font-black">Inventory</h2><Link href="/admin/products" className="text-xs font-bold uppercase tracking-widest underline">Manage products</Link></div><div className="mt-5 flex flex-col gap-3">{productRows.slice(0,8).map(product=><Link key={product.id} href={`/admin/products/${product.id}/edit`} className="flex items-center justify-between border-b border-border pb-3"><div><p className="font-bold">{product.name}</p><p className="text-xs text-muted-foreground">{product.size} · {product.condition}</p></div><span className={`text-xs font-bold uppercase ${product.stock?"text-accent":"text-destructive"}`}>{product.stock?"Live":"Sold"}</span></Link>)}</div><Link href="/admin/products/new" className="mt-5 block text-center text-xs font-bold uppercase tracking-widest underline">Add product</Link></div></div></section>
}
